function stringLenght(first, second, thrid){

    let result = first.length + second.length + thrid.length;
     console.log(result);
     console.log(Math.trunc(result / 3));
 
 }
 stringLenght('chocolate', 'ice cream', 'cake');